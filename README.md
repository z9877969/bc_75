# Module_02. lesson_1 
# * Події. Стан компонента. *

## 1. Обробка подій. 
`-` посилання на функцію \
`-` об'єкт події \
`-` пропси в обробці подій

## 2. Стан
`-` запускає ререндер \
`-` зберігає мінімальну к-сть даних обчислювані дані \
`-` односпрямований потік даних


## 3. useState
`-` декілька станів
`-` ізоляція станів - стан локальний для одного компонента
`-` підняття стану
`-` оновлення об'єктів

## 4. хуки в умовах
