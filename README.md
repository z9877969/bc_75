# Module_04. lesson_2

# _ Hooks _


## 1. useMemo.
`-` мемоізація

## 2. useRef.
`-` доступ до DOM вузлів \
`-` принцип запису значення в ref \
`-` ефект фокус \
`-` збереження значення між рендерами

## 3. Власні хуки
`-` інкапсуляція логіки пов'язаної з вбудованими хуками реакта \
`-` useLocalStorage 

## 4. Контекст
`-` меню навігації
`-` todo context