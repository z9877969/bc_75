# Module_03. lesson_1

# _ Форми _

## 1. Неконтрольовані форми.

`-` для чого та як використовуються \
`-` внутрішній інтерфейс: elements | reset

## 2. useId

`-` створення id для унікальності елементів

## 3. Контрольовані елементи

`-` select \
`-` date \
`-` radio \
`-` checkbox \
`-` text

## 4. Контрольована форма

## 5. Колекція елементів

`-` додавання елементу \
`-` видалення елементу \
`-` фільтр елементів
